import _logger from './logger';
import {
  firsLetterUpperRestLower as fluprl,
  parseName as ps,
} from './parse_name';

export const logger = _logger;
export const firsLetterUpperRestLower = fluprl;
export const parseName = ps;
